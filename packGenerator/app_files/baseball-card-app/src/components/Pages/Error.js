import React from 'react';
import "./Error.css";
const Error = props => {
    return(
    <div className = "error">
        <h1>Page does not exist. yet...</h1>
    </div>
    );
};

export default Error